# VOX — Environment Configuration
# Copy this to .env and fill in your values
# NEVER commit .env — it's gitignored

# Picovoice (wake word detection)
# Get a free key at https://console.picovoice.ai/
PORCUPINE_ACCESS_KEY=your_access_key_here

# Ollama (should be running locally)
OLLAMA_HOST=http://127.0.0.1:11434
# Recommended models — pick an UNCENSORED model for full persona personality:
#   mythomax:13b       — 9GB VRAM, best personality/roleplay (default, recommended)
#   dolphin-llama3:8b  — 5GB VRAM, zero refusal, great VRAM headroom
#   dolphin-mistral-nemo:12b — 8GB VRAM, strong balance
#   qwen2.5:14b        — 9GB VRAM, good general but has alignment refusals
OLLAMA_MODEL=mythomax:13b
n# Chat personality model (optional — used for pure conversation, falls back to OLLAMA_MODEL)
# Use an uncensored personality-focused model for best results:
#   samantha-mistral   — 4GB VRAM, empathetic personality, great for companionship
#   dolphin-mistral    — 4GB VRAM, zero refusal, good all-rounder
# OLLAMA_CHAT_MODEL=samantha-mistral

# Vision model (for analyzing uploaded images)
# Requires: ollama pull llava
VISION_MODEL=llava

# User profile
# USER_EMAIL — default recipient for "email me" / "send me" (no address needed)
# USER_EMAIL=you@example.com

# Email (optional — for send_email tool)
# Supports any SMTP server: local relay, Gmail, SES, etc.
# Auto-detects STARTTLS (uses it if available, skips for plaintext relays)
# SMTP_HOST=your-relay.local
# SMTP_PORT=587
# SMTP_USER=your_smtp_user
# SMTP_PASSWORD=your_smtp_password
# SMTP_FROM=vox@yourdomain.com

# Home Assistant (optional)
# HASS_URL=http://127.0.0.1:8123
# HASS_TOKEN=your_long_lived_access_token_here

# Audio device indices (run `vox --list-devices` to find yours)
# MIC_DEVICE_INDEX=0
# SPEAKER_DEVICE_INDEX=0

# STT (Faster-Whisper)
# WHISPER_MODEL=base.en
# WHISPER_DEVICE=cuda

# TTS engine: "piper" (fast) or "xtts" (voice cloning)
# TTS_ENGINE=piper

# Image generation (optional — requires pip install -e ".[image]")
# IMAGE_MODEL=stabilityai/stable-diffusion-xl-base-1.0
# IMAGE_MODEL_NSFW=RunDiffusion/Juggernaut-X-v10
# IMAGE_NSFW_FILTER=on
# IMAGE_STEPS=30
# IMAGE_WIDTH=1024
# IMAGE_HEIGHT=1024
# IMAGE_NEGATIVE_PROMPT=deformed, bad anatomy, extra limbs, mutated hands, poorly drawn face, blurry, low quality, watermark, text, signature, cropped

# HuggingFace cache location (defaults to models/huggingface inside project)
# HF_HOME=C:/vox/models/huggingface

# Persona (optional — gives VOX a visual identity and personality)
# When set, VOX becomes a character with her own life, moods, and appearance.
# Character cards in persona/ directory provide deeper personality definition.
# VOX_PERSONA_NAME=Luna
# VOX_PERSONA_DESCRIPTION=a young woman with long dark brown hair, green eyes, fair skin
# VOX_PERSONA_STYLE=photorealistic, natural lighting, high detail, 8k

# Web UI (optional — requires pip install -e ".[web]")
# Start with: vox --web
# WEB_HOST=0.0.0.0
# WEB_PORT=8080
# WEB_AUTH_USER=          # HTTP Basic Auth username (leave empty to disable)
# WEB_AUTH_PASS=          # HTTP Basic Auth password

# Character Card (optional — YAML file for rich persona with backstory, moods, memory)
# See persona/example.yaml for the full template
# VOX_PERSONA_CARD=persona/my-persona.yaml

# Claude API (optional — smart escalation for complex tasks)
# Uses Haiku by default (~$0.001/request). Only called when local LLM can't handle it.
# Get a key at https://console.anthropic.com/
# CLAUDE_API_KEY=sk-ant-xxxxx
# CLAUDE_MODEL=claude-haiku-4-5-20251001

# GitHub (optional — auto-creates issues when users request unimplemented features)
# GITHUB_REPO=your-username/vox

# Search backend: ddg (default, no key), brave (API key needed), searxng (self-hosted)
# SEARCH_ENGINE=ddg
# BRAVE_API_KEY=your_brave_api_key_here
# SEARXNG_URL=http://your-searxng-instance:8080
